# OS
.DS_Store
Thumbs.db

# Node / Astro
node_modules/
dist/
.astro/
.output/
.vercel/
.netlify/

# Env & secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*

# Editor
.vscode/
.idea/

# Temp
*.tmp
/tmp/

# Raw downloaded asset packs (curate into brand/assets/ instead — never commit raw zips)
*.zip

# Raw video/image masters (keep local, never commit — large stock source files)
/*.mov
/hf_*.mp4
*.mhtml
brand/motion/masters/

# Internal HPP / cost reports — keep local, never push to the site repo
/reports/
